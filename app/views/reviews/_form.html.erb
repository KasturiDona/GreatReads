<!-- form for adding/editing a review -->
<%= form_for @review, :url => @url do |f| %>

	<div class="form-group">
		<%= f.label :description %>
		<%= f.text_area :description, placeholder: "Add a review", :class=> "form-control" %>
	</div>
	
	<div class="form-group">
		<%= f.label :rating %>
		<%= f.number_field :rating, in: 1..5, placeholder: "Rate the book", :class=> "form-control" %>
	</div>

	<%= f.submit :class => 'submit btn btn-primary'%>

<% end %>

