<%= form_for @review, :url => book_review_path( @review.book, @review ) do |f| %>

<p>
	<%= f.label :description %>
	<%= f.text_field :description %>
</p>
<p>
	<%= f.label :rating %>
	<%= f.text_field :rating %>
</p>

<%= f.submit %>

<% end %>