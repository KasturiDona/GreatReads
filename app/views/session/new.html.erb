<h2>Login</h2>

<%= form_for '/login' do |f| %>
	<p>
		<%= f.label :email %>
		<%= f.text_field :email %>
	</p>
	<p>
		<%= f.label :password %>
		<%= f.password_field :password %>
	</p>
	</p>
		<%= f.label :login_to_great_reads %>
		<%= f.submit :class => 'submit' %>
	</p>
<% end %>